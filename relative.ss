#!/usr/bin/petite --script

'(
  (p
    (pro (proce
	  (process))
	 (produ
	  (product))
	 (progr
	  (progress))
	 (proje
	  (project))
	 (promi
	  (promise))
	 (prono
	  (pronounce))))
  (r
   (rea
    (reall
     (really)))
   (rel
    (relat
     (relative)))
   (rep
    (repre
     (represent)))
   (rom
    (roman
     (romania)))))
